package com.project.dirtywhrere.ui.screens

import androidx.compose.foundation.layout.Box
import androidx.compose.material3.Text
import androidx.compose.runtime.Composable

@Composable
fun DetailsScreen(id : String?) {
    // Implement the Add Screen UI

    // The Add Screen should have a text field for the user to input the name of the gate
    // The Add Screen should have a switch to indicate the status of the gate
    // The Add Screen should have a switch to indicate the status of the gate (clean or dirty)
    // The Add Screen should have a button to add the gate to the list of gates
    // The Add Screen should have a button to cancel the addition of the gate
    Box {
        Text("Details Screen for gate: $id")
    }
}